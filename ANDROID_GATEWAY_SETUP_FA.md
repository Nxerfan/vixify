# راهنمای راه‌اندازی دروازه اندروید

> ## ⚠️ سند مختص اپراتور — ابتدا این را بخوانید
>
> **این راهنما فقط برای اپراتورهای پلتفرم Vixify است.**
>
> مشتریان عادی Vixify **نیازی به دنبال کردن این راهنما ندارند**، به **هیچ
> سخت‌افزاری** نیاز ندارند و نباید هیچ گوشی یا سیم‌کارتی را پیکربندی کنند.
>
> Vixify یک **سرویس مدیریت‌شده او‌تی‌پی** است. مشتریان فقط API/SDK را ادغام
> می‌کنند و به ازای او‌تی‌پی‌هایی که می‌فرستند پرداخت می‌کنند — Vixify تمام
> زیرساخت پیامک را از طریق دروازه واحدِ نگهداری‌شده توسط اپراتور (که در ادامه
> توضیح داده شده) به نمایندگی از آن‌ها مدیریت می‌کند.
>
> اگر توسعه‌دهنده‌ای هستید که از Vixify برای ارسال او‌تی‌پی به کاربران خود
> استفاده می‌کنید، می‌توانید از اینجا دست نگه دارید و به
> [مستندات API](#مرجع-api-برای-مشتری) در انتهای این فایل مراجعه کنید.
>
> **اگر اپراتور Vixify هستید** (شخصی که پلتفرم Vixify را اجرا می‌کند)، ادامه
> مطلب را بخوانید تا یاد بگیرید چگونه گوشی دروازه‌ای که به همه مشتریان Vixify
> خدمت می‌رسد را راه‌اندازی و نگهداری کنید.

---

## نمای کلی

Vixify از ارائه‌دهنده پیامک سنتی ثالث استفاده نمی‌کند. در عوض، یک **گوشی
اندروید واحد با سیم‌کارت فعال** — که توسط اپراتور Vixify نگهداری می‌شود —
به‌عنوان دروازه پیامک برای **همه** مشتریان عمل می‌کند.

جریان کاملاً ناهمگام است:

1. یک مشتری `POST /api/request-otp` را فراخوانی می‌کند.
2. Vixify یک کد ۶ رقمی تولید می‌کند، آن را با وضعیت `pending` ذخیره می‌کند و
   فوراً `200 OK` برمی‌گرداند. فراخوانی API مشتری هرگز روی تأخیر مخابراتی
   مسدود نمی‌شود.
3. گوشی اندرویدی اپراتور یک اپ اتوماسیون کوچک (MacroDroid یا Tasker) اجرا
   می‌کند که هر ۱۰ ثانیه `GET /api/v1/get-pending-sms` را **نظرسنجی** می‌کند.
4. وقتی یک او‌تی‌پی در حال انتظار برگردانده شد، گوشی **پیامک را** با سیم‌کارت
   خود به گیرنده ارسال می‌کند.
5. پس از ارسال، گوشی `POST /api/v1/confirm-sent` را فراخوانی می‌کند تا وضعیت
   او‌تی‌پی را از `pending` به `sent` به‌روزرسانی کند. وب‌هوک‌های (`otp.sent`)
   در این نقطه اجرا می‌شوند.

این مدل به Vixify تحویل مستقیم مخابراتی، هزینه صفر ثالث به ازای هر پیام، و
بدون وابستگی به ارائه‌دهنده می‌دهد — در ازای نگهداری یک دستگاه همیشه‌روشن
توسط اپراتور.

---

## پیش‌نیازها

| نیازمندی | توضیحات |
|---|---|
| گوشی اندروید | اندروید ۸.۰ (Oreo) یا جدیدتر. یک دستگاه اختصاصی، همیشه‌در‌حال‌شارژ و همیشه‌آنلاین به‌شدت توصیه می‌شود. |
| سیم‌کارت فعال | پس‌پرداخت (اعتباری دائمی) ترجیح دارد تا از اتمام اعتبار جلوگیری شود. باید کشورهای مقصد مورد نظر را پشتیبانی کند. |
| اینترنت پایدار | گوشی باید در همه اوقات از طریق وای‌فای یا دیتای موبایل به API Vixify دسترسی داشته باشد. |
| MacroDroid **یا** Tasker | اپ اتوماسیون برای نظرسنجی API و ارسال پیامک. MacroDroid برای سهولت راه‌اندازی توصیه می‌شود. |
| `GATEWAY_SECRET` | یک رمز مشترک که توسط بک‌اند Vixify صادر می‌شود. در هدر `x-gateway-secret` در هر فراخوانی دروازه ارسال می‌شود. |

---

## ۱. دریافت رمز دروازه

رمز دروازه، گوشی را به API Vixify احراز هویت می‌کند. این رمز به‌عنوان متغیر
محیطی `GATEWAY_SECRET` روی سرور Vixify تنظیم می‌شود و باید با هدر
`x-gateway-secret` ارسال‌شده توسط گوشی مطابقت داشته باشد.

```bash
# روی سرور Vixify (.env)
GATEWAY_SECRET="<یک رشته تصادفی طولانی>"
```

یک رمز قوی تولید کنید، مثلاً:

```bash
openssl rand -hex 32
```

این را به‌طور امن ذخیره کنید. هر کسی با این رمز می‌تواند او‌تی‌پی‌های در حال
انتظار را بخواند و ارسال‌ها را تأیید کند — با آن مانند یک اعتبار عملیاتی
رفتار کنید.

---

## ۲. راه‌اندازی گوشی اندروید

### ۲.۱ آماده‌سازی دستگاه

1. یک گوشی اختصاصی را به تنظیمات کارخانه برگردانید (توصیه می‌شود) یا از یک
   دستگاه تمیز استفاده کنید.
2. سیم‌کارت فعال را وارد کنید و تأیید کنید که می‌تواند به‌صورت دستی پیامک
   ارسال کند.
3. به یک شبکه وای‌فای قابل‌اعتماد متصل شوید (دیتای موبایل به‌عنوان پشتیبان).
4. بهینه‌سازی باتری را برای اپ اتوماسیون انتخاب‌شده غیرفعال کنید — این
   حیاتی است، در غیر این صورت اندروید حلقه نظرسنجی را متوقف می‌کند.
5. گوشی را همیشه به برق متصل نگه دارید.

### ۲.۲ نصب اپ اتوماسیون

یکی از موارد زیر را نصب کنید:

- **MacroDroid** (توصیه می‌شود) — رایگان، رابط کاربری ساده‌تر، برای این کار
  کافی است.
  - Google Play: جستجوی "MacroDroid"
  - نسخه بدون تبلیغات اختیاری است.
- **Tasker** (پولی) — قدرتمندتر، یادگیری سخت‌تر.
  - Google Play: جستجوی "Tasker"

---

## ۳. پیکربندی ماکرو نظرسنجی (MacroDroid)

یک ماکرو جدید با منطق زیر ایجاد کنید. ماکرو روی تایمر ۱۰ ثانیه‌ای اجرا
می‌شود، endpoint دریافت در حال انتظار را فراخوانی می‌کند و — اگر او‌تی‌پی در
حال انتظاری وجود داشت — پیامک را ارسال کرده و تأیید می‌کند.

### تریگر (Trigger)

- **Timer / Repeat**: هر `10 seconds`

### اکشن ۱ — HTTP GET او‌تی‌پی در حال انتظار

| فیلد | مقدار |
|---|---|
| Method | `GET` |
| URL | `https://vixify.vercel.app/api/v1/get-pending-sms` |
| Header | `x-gateway-secret: <YOUR_GATEWAY_SECRET>` |
| Content type | `application/json` |
| ذخیره پاسخ در | متغیر محلی `response` |

### اکشن ۲ — تجزیه و ارسال (مشروط)

یک **شرط** اضافه کنید: بدنه پاسخ خالی نیست / با `{"pending":null}` برابر
نیست.

وقتی یک او‌تی‌پی در حال انتظار وجود دارد، پاسخ به این شکل است:

```json
{
  "pending": {
    "id": "otp_01HZX...",
    "phone": "+989120000000",
    "code": "482913",
    "message": "Your Vixify code is 482913 — Vixify"
  }
}
```

JSON را تجزیه کنید (*Variable Operation → Parse JSON* در MacroDroid یا یک
قطعه JavaScript) و استخراج کنید:

- `id` → در `otp_id`
- `phone` → در `target_phone`
- `message` → در `sms_body`

### اکشن ۳ — ارسال پیامک

| فیلد | مقدار |
|---|---|
| Action | **Send SMS** |
| Number | `{target_phone}` |
| Message | `{sms_body}` |

> **مهم:** فیلد `message` را دقیقاً همان‌طور که دریافت شده ارسال کنید. Vixify
> پیام کامل را سمت سرور می‌سازد، شامل کد و هر برندینگ/امضایی که توسط طرح
> فرستنده اعمال می‌شود. متن را تغییر ندهید.

### اکشن ۴ — تأیید ارسال (HTTP POST)

| فیلد | مقدار |
|---|---|
| Method | `POST` |
| URL | `https://vixify.vercel.app/api/v1/confirm-sent` |
| Header | `x-gateway-secret: <YOUR_GATEWAY_SECRET>` |
| Content type | `application/json` |
| Body | `{"id":"{otp_id}"}` |

این کار او‌تی‌پی را به‌عنوان `sent` علامت‌گذاری می‌کند و وب‌هوک `otp.sent` را
به مشتری شلیک می‌کند.

### اکشن ۵ — مدیریت خطا

اگر اکشن ارسال پیامک شکست بخورد (بدون سیگنال، شماره نامعتبر)، با بدنه زیر به
`/api/v1/confirm-sent` یک POST بزنید:

```json
{ "id": "{otp_id}", "status": "failed", "error": "<دلیل>" }
```

این شکست را ثبت می‌کند تا وب‌هوک مشتری (`otp.failed`) اجرا شود و دفتر تحویل
وضعیت واقعی را منعکس کند.

---

## ۴. پیکربندی با Tasker (جایگزین)

اگر Tasker را ترجیح می‌دهید، پروفایل معادل این است:

1. **Profile → Time → Repeat Every**: `10` ثانیه.
2. **Task → Net → HTTP Request**:
   - Method: `GET`
   - URL: `https://vixify.vercel.app/api/v1/get-pending-sms`
   - Headers: `x-gateway-secret:<YOUR_GATEWAY_SECRET>`
   - Output: `%response`
3. **Task → If** `%response` شامل `"pending":null` نیست:
   - **Variable Split / JSON Read** برای استخراج `id`, `phone`, `message`.
   - **Phone → Send SMS** به `%phone` با `%message`.
   - **Net → HTTP Request** `POST` به `/api/v1/confirm-sent` با
     `{"id":"%id"}`.
4. **Else** کاری نکنید (هیچ او‌تی‌پی در حال انتظاری نیست).
5. نوتیفیکیشن پیش‌زمینه Tasker را تنظیم کنید و بهینه‌سازی باتری را غیرفعال
   کنید.

---

## ۵. سلامت و پایش

گوشی دروازه، یک نقطه شکست واحد برای کل پلتفرم است. آن را به‌طور فعال
پایش کنید:

- **بررسی آپ‌تایم:** داشبورد Vixify باید یک مهر زمانی «آخرین نظرسنجی دروازه»
  را نمایش دهد. اگر هیچ نظرسنجی‌ای در عرض ۳۰ ثانیه انجام نشد، به اپراتور
  هشدار دهید.
- **باتری:** دستگاه را در حال شارژ نگه دارید؛ یک UPS روی شارژر توصیه می‌شود.
- **اتصال:** وای‌فای پایدار را تأیید کنید؛ یک fallback دیتای موبایل پیکربندی
  کنید.
- **موجودی سیم‌کارت:** اگر پیش‌پرداخت است، یک هشدار موجودی کم با مخابرات خود
  تنظیم کنید.
- **نسبت ارسال/شکست:** اگر نرخ شکست بالا رفت، ممکن است سیم‌کارت مسدود یا بدون
  اعتبار باشد — فوراً بررسی کنید.
- **محدودیت‌های نرخ پیامک:** مخابرات ممکن است فرستندگان پرحجم را محدود کند.
  اگر به محدودیت‌های مخابرات نزدیک شدید، موازنه بار روی چند گوشی دروازه را
  در نظر بگیرید (پیشرفته، نیاز به تغییرات بک‌اند دارد).

---

## ۶. نکات امنیتی

- `GATEWAY_SECRET` دسترسی خواندن کامل به او‌تی‌پی‌های در حال انتظار می‌دهد.
  هرگز آن را در git کامیت نکنید، هرگز در کد سمت کلاینت جاسازی نکنید، و اگر
  گوشی گم یا آسیب دید آن را چرخش دهید.
- گوشی باید با PIN/بیومتریک **قفل** شود و از نظر فیزیکی امن نگه داشته شود.
- اگر گوشی IP ثابت دارد، endpointهای دروازه را بر اساس IP محدود کنید (دفاع
  در عمق، اختیاری).
- گوشی فقط از طریق اپ اتوماسیون پیامک **ارسال** می‌کند — هرگز نباید به‌عنوان
  دستگاه شخصی استفاده شود تا از تداخل تصادفی با حلقه نظرسنجی جلوگیری شود.

---

## ۷. رفع اشکال

| علامت | علت محتمل | راه‌حل |
|---|---|---|
| او‌تی‌پی‌ها برای همیشه `pending` می‌مانند | گوشی آفلاین، ماکرو متوقف شده، بدون اینترنت | بررسی کنید دستگاه شارژ می‌شود، وای‌فای متصل است، MacroDroid بهینه‌سازی باتری نشده |
| او‌تی‌پی‌ها `sent` علامت‌گذاری می‌شوند اما کاربران دریافت نمی‌کنند | سیم‌کارت بدون اعتبار، مسدودسازی مخابرات، فرمت شماره اشتباه | موجودی سیم‌کارت را بررسی کنید، یک پیامک دستی تست کنید، فرمت E.164 را تأیید کنید |
| `401`/`403` روی endpointهای دروازه | `GATEWAY_SECRET` اشتباه یا چرخش‌یافته | رمز را دوباره صادر کنید، هم `.env` سرور و هم هدر ماکرو را به‌روز کنید |
| نظرسنجی کار می‌کند اما پیامکی ارسال نمی‌شود | مجوز پیامک MacroDroid لغو شده | مجوز پیامک را در تنظیمات اندروید دوباره اعطا کنید |
| ارسال پیامک تکراری | نظرسنجی سریع‌تر از رفت‌وبرگشت تأیید | مطمئن شوید confirm-sed بلافاصله پس از ارسال اجرا می‌شود؛ فاصله نظرسنجی را به زیر ۱۰ ثانیه کاهش ندهید |

---

## مرجع API برای مشتری

> endpointهای زیر API **عمومی، رو به مشتری** هستند. مشتریان از این‌ها با کلید
> API خود (احراز هویت Bearer) استفاده می‌کنند — آن‌ها هرگز به endpointهای
> دروازه بالا دست نمی‌زنند.

| Endpoint | احراز هویت | هدف |
|---|---|---|
| `POST /api/request-otp` | `Authorization: Bearer <API_KEY>` | درخواست ارسال یک کد ۶ رقمی به یک شماره تلفن |
| `POST /api/verify-otp` | `Authorization: Bearer <API_KEY>` | تأیید کدی که کاربر وارد کرده |
| `GET /api/usage` | `Authorization: Bearer <API_KEY>` | دریافت مصرف و سهمیه باقی‌مانده |

endpointهای دروازه (`/api/v1/get-pending-sms`, `/api/v1/confirm-sent`)
**داخلی** هستند و به هدر `x-gateway-secret` نیاز دارند — مشتریان هرگز
آن‌ها را فراخوانی نمی‌کنند.

---

*بخشی از پلتفرم Vixify — یک محصول مدیریت‌شده او‌تی‌پی به‌عنوان سرویس. مشتریان
SDK `@nixify/sms` یا REST API را ادغام می‌کنند و فقط به ازای او‌تی‌پی‌هایی که
می‌فرستند پرداخت می‌کنند؛ بدون سخت‌افزار، بدون قرارداد مخابراتی، بدون
حاشیه‌سود به ازای هر پیام.*
